# Generated by seaweed-up plan from one_host_dev.inventory.yaml on 2026-05-01T12:00:00Z.
# Safe to hand-edit. Re-running plan against this file currently
# refuses to overwrite unless --overwrite is passed; Phase 3 will add
# append-merge that preserves comments and hand edits.
#
# NOTE: filer_servers[*].config is empty. Fill in the metadata-store
# credentials before deploy, or re-run plan with --filer-backend.

cluster_name: dev
global:
    dir.conf: /etc/seaweed
    dir.data: /opt/seaweed
    volumeSizeLimitMB: 5000
    replication: "000"
master_servers:
    - ip: 10.0.0.1
      port.ssh: 22
      port: 9333
      port.grpc: 19333
volume_servers:
    - ip: 10.0.0.1
      port.ssh: 22
      port: 8080
      port.grpc: 18080
      folders:
        - folder: /data1
          disk: ssd
          max: 19
filer_servers:
    - ip: 10.0.0.1
      port.ssh: 22
      port: 8888
      port.grpc: 18888
